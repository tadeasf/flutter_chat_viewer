# # This file was auto-generated by the Firebase CLI
# # https://github.com/firebase/firebase-tools

# name: Deploy to Firebase Hosting on PR
# on: pull_request
# permissions:
#   checks: write
#   contents: read
#   pull-requests: write

# jobs:
#   build_and_preview:
#     if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4

#       - name: Set up Flutter
#         uses: subosito/flutter-action@v2
#         with:
#           flutter-version: '3.22.3'
#       - name: Install dependencies
#         run: flutter pub get

#       - name: Build Flutter Web App
#         run: flutter build web --release

#       - name: Deploy to Firebase Hosting for Preview
#         uses: FirebaseExtended/action-hosting-deploy@v0
#         with:
#           repoToken: ${{ secrets.GITHUB_TOKEN }}
#           firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_FORT_ON_CLOUD }}
#           projectId: fort-on-cloud
#           channelId: pr-${{ github.event.pull_request.number }}  # Unique channel ID for each PR
